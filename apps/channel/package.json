{
  "name": "channel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The agent, living in a Slack or Teams thread, via CopilotKit Channels.",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx --watch src/server.ts",
    "start": "node --env-file=../../.env --import tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'src/**/*.test.tsx'"
  },
  "dependencies": {
    "@ag-ui/client": "0.0.59",
    "@ag-ui/core": "0.0.59",
    "@copilotkit/channels": "0.9.2",
    "@copilotkit/runtime": "1.70.3",
    "agent-core": "workspace:*",
    "rxjs": "7.8.1",
    "zod": "^4.1.0"
  }
}
